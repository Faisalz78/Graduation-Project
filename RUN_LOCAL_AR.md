# تشغيل المشروع وتجربته

آخر تحقق: 14 سبتمبر 2026. الإصدار `0.19.0` يعمل محليًا على Windows، بما فيه عامل الاستخراج والتدقيق والشراء والحوكمة واللوحة والميزانيات، وإدارة المشاريع والمستخدمين والمقارنة الإقليمية والتنبيهات وتقارير CSV من الواجهة. تطبيق Expo الأولي باقٍ على نطاق `0.11.0` ومجمد مؤقتًا.

## التشغيل اليومي

بدءًا من `0.18.0` يتحقق تشغيل المشروع من Ollama والنموذج المحلي `qwen3-vl:8b-instruct` أيضًا. النموذج محمل على هذا الجهاز. عند غيابه يستمر استخراج النص مع تنبيه في شاشة المراجعة. لا تستخدم الفواتير خدمة سحابية؛ تحميل الأوزان لأول مرة فقط يحتاج اتصالًا.

1. افتح مجلد `Graduation-Project-AI-Invoice-Audit` على سطح المكتب.
2. شغّل `Start_Project.cmd`. ينتظر جاهزية الخادم والواجهة، ثم يفتح المتصفح.
3. افتح الملف المولد محليًا `.local/DEMO_ACCOUNTS.md`، واستخدم `employee@demo.test` وكلمة المرور المكتوبة فيه.
4. اختر «رفع فاتورة»، ثم المشروع وملف PDF أو JPEG أو PNG أو UBL XML، واضغط «حفظ المسودة».
5. افتح المسودة من القائمة، وجرّب «تحميل الملف الأصلي».
6. اختر «تعديل بيانات الفاتورة»، وأضف المورد ورقم الفاتورة وتاريخها والعملة عند توفرها.
7. اضغط «إضافة بند»، وأدخل الوصف والكمية وسعر الوحدة قبل الضريبة والخصم ونسبة الضريبة. أدخل أيضًا الإجماليات الظاهرة على أصل الفاتورة عندما تكون واضحة، ثم احفظ.
8. لتجربة الميزانية، ادخل بحساب المالية وافتح «ميزانيات المشاريع». اختر المشروع والعملة، وأنشئ فئة وبندًا ومخصصًا ثم احفظ. عد إلى الموظف واختر بند الميزانية داخل كل بند فاتورة. يعرض مدير المشروع الأرقام دون صلاحية تعديلها.
9. لتجربة الإدارة، ادخل بحساب المالية وافتح «إدارة النظام». أنشئ حسابًا بكلمة مرور مؤقتة ومشروعًا، ثم اختر الموظف ومدير المشروع من أعضاء المشروع واحفظ. يمكن تعديل الدور والحالة وإعادة ضبط كلمة مرور حساب آخر؛ تغلق إعادة الضبط جلساته السابقة.
10. لتجربة المطابقة، ادخل بحساب المالية وأنشئ أمرًا من «أوامر الشراء»، ثم ادخل بحساب الموظف وسجّل الاستلام مع ملف الإثبات. في تعديل الفاتورة اختر الأمر واربط كل بند ببنده.
11. من التفاصيل راجع بطاقات التدقيق التي تقارن الأصل والحساب وQR/XML وأمر الشراء والكميات المستلمة والميزانية. بعد توفر ثلاث فواتير سابقة مرسلة لنفس المورد والعملة والبند، تعرض بطاقة الأسعار الوسيط وحد التنبيه. تعرض بطاقة التكرار التقريبي أعلى درجة وعواملها. افتح «عرض التغييرات» في السجل للمقارنة بين القيم قبل وبعد التعديل.
12. لإظهار المقارنة الإقليمية، حدد منطقة المورد وأنشئ تاريخًا مرسلًا لبنود متقاربة في الوصف والوحدة والعملة ضمن منطقتين على الأقل. تعرض البطاقة وسيط منطقة المورد ووسائط المناطق وحجم العينة والثقة وفرق التكلفة، أو تشرح سبب عدم كفاية البيانات.
13. أكمل المورد ورقم الفاتورة والتاريخ والعملة وبندًا واحدًا على الأقل. اضغط «إرسال للمراجعة»، وأكد مطابقة البيانات للأصل ثم «تأكيد الإرسال».
14. ادخل بحساب `manager@demo.test` لمشروع المقر الرئيسي، أو `manager2@demo.test` لمشروع المستودع. يظهر الإرسال في «التنبيهات» مع عداد غير المقروء. افتح الفاتورة واختر «موافقة وإرسال للمالية» ثم «تأكيد الموافقة».
15. ادخل بحساب `finance@demo.test`، وافتح «حدود الموافقات» لمراجعة حد كل مدير وعملة. لا يستطيع مدير المالية تعديل حده بنفسه؛ استخدم `finance2@demo.test` عند الحاجة. افتح الفاتورة واختر «اعتماد الفاتورة» ثم «تأكيد الموافقة». تظهر «معتمدة» ويبقى كل قرار في السجل.
16. افتح «التقارير» بحساب المدير أو المالية، واضبط المرشحات ثم نزّل سجل الفواتير أو ملخص الميزانيات أو المقارنة الإقليمية بصيغة CSV.
17. لتجربة التصحيح استخدم فاتورة أخرى واختر «طلب تعديل» من حساب المراجع، واكتب السبب. يعود الموظف إلى «تعديل بيانات الفاتورة»، يحفظ ثم يعيد الإرسال؛ تبدأ مراجعة المشروع من جديد ويصل تنبيه بالنتيجة. «رفض الفاتورة» يتطلب سببًا ويغلقها دون إعادة فتح في هذا الإصدار.

قائمة المدير تبدأ بفواتير مرحلته المنتظرة. اختر «كل الحالات» أو الحالة المطلوبة من «تصفية بالحالة» للاطلاع على التاريخ. اقرأ [قواعد المراجعة](02_Requirements/Use_Cases/INVOICE_WORKFLOW_AR.md) لفهم الخصوصية والتعديل والنسخ.

لا يختار النظام نسبة الضريبة تلقائيًا. العملات الحالية SAR وAED وUSD وEUR؛ الإجماليات من منزلتين، والخصم مبلغ على البند. يمكنك حفظ رأس المسودة دون بنود؛ تظهر الإجماليات بعد إدخالها. [سياسة الحساب والحدود](02_Requirements/Use_Cases/INVOICE_DATA_AR.md).

يمكن استخدام الملف المصطنع الموجود في `.local/samples/demo-invoice.pdf`. الفواتير التي تحمل كلمة «تجريبي» في القائمة أُنشئت أثناء الاختبار، وليست مطالبات مالية حقيقية.

لإيقاف الخدمات شغّل `Stop_Project.cmd`. تبقى قاعدة البيانات والملفات محفوظة. عند تعديل كود Python أوقف المشروع وشغّله مجددًا؛ واجهة Next.js تحدث نفسها أثناء التطوير.

| الرابط | الاستخدام |
|---|---|
| http://127.0.0.1:3000 | الواجهة العربية. استخدم هذا العنوان نفسه عند تسجيل الدخول. |
| http://127.0.0.1:8000/api/docs | مرجع API التفاعلي للمطور. |
| http://127.0.0.1:8000/api/v1/health | فحص اتصال الخادم بقاعدة البيانات. |

الدخول إلى الويب من `localhost` بدل `127.0.0.1` يحتاج تعديل `APP_ORIGIN`؛ النسخة الحالية تضبط مصدر طلبات Cookie على العنوان المذكور. تطبيق Expo يستخدم `10.0.2.2` افتراضيًا لمحاكي Android، وتفاصيل الهاتف الفعلي في [دليل الجوال](04_Source_Code/mobile/README_AR.md).

## ما يمكنك تجربته الآن

- تسجيل الدخول والخروج بحسابات الموظف ومدير المشروع ومدير المالية.
- عرض المشاريع بحسب دور المستخدم وتكليفه.
- رفع مسودة خاصة بالموظف مع المشروع والملاحظة والملف الأصلي.
- قائمة بصفحات، وتفاصيل المسودة، وتنزيل الأصل، وسجل الإنشاء والإرفاق.
- منع المستخدم الآخر من فتح المسودة أو ملفها، حتى عند معرفة الرابط.
- إدخال بيانات الفاتورة وبنودها وتعديلها، ومعاينة الإجماليات وحفظ النسخة الجديدة وسجل قيمها.
- إدخال إجماليات الأصل أو نقلها من OCR أو QR أو XML، ثم مشاهدة التطابق أو الفرق وكشف التكرار ومقارنة المصدر المنظم مع السبب.
- إنشاء أمر شراء من حساب المالية، وتسجيل استلام بإثبات خاص، وربط بنود الفاتورة ومراجعة المطابقة الثلاثية.
- إنشاء فئات مصروفات وميزانية مستقلة لكل مشروع وعملة، وربط بنود الفاتورة ومراجعة المعتمد والملتزم وقيد المراجعة والمتبقي.
- إنشاء الحسابات والمشاريع وتعديل الأدوار والحالات وعضويات المشاريع وإعادة كلمات المرور من «إدارة النظام» بحساب مدير المالية.
- مقارنة أسعار الموردين إقليميًا من تاريخ الشركة الداخلي عند اكتمال العينة، مع إظهار الوسائط والثقة وسبب تعذر المقارنة عند نقص البيانات.
- استقبال تنبيهات الإرسال والقرارات في صندوق خاص بالمستخدم، وقراءتها فرديًا أو دفعة واحدة.
- تنزيل تقارير CSV للفواتير والميزانيات والمقارنة الإقليمية من «التقارير» بصلاحيات المدير والمالية.
- منع نافذة قديمة من الكتابة فوق نسخة أحدث. عند التعارض تبقى مدخلاتك حتى تختار تحميل النسخة الأحدث وإلغاء تعديلاتك.

لا تظهر المسودات الخاصة للمديرين حتى يرسلها الموظف. بعد الإرسال يتوقف التعديل حتى يطلبه المراجع. الفاتورة المعادة للتعديل تظل متاحة للمراجعين ضمن النطاق. لا يستطيع صاحب الفاتورة مراجعتها، ولا يعتمد الحساب نفسه مرحلتي المشروع والمالية. كل قرار يتطلب أحدث نسخة؛ تبقى تعليقات النافذة القديمة عند التعارض حتى تحمل البيانات الجديدة.

من صفحة «الموردون» يستطيع مدير المالية وضع المورد قيد المراجعة أو اعتماده أو رفضه داخليًا مع ملاحظة. هذا لا يمثل تحققًا رسميًا. قراءة QR/XML لا تتحقق من التوقيع أو حالة ZATCA ولا تثبت الأصالة. حدود الموافقة بحسب الفئة والمخاطر والتصعيد لم تُنفذ بعد، والاعتماد المسجل لا ينفذ دفعًا. تعريف أرقام الميزانية موثق في [مواصفات الميزانية](02_Requirements/Use_Cases/PROJECT_BUDGETS_AR.md).

لتجربة إشعار دائن أو مدين، أنشئ أولًا فاتورة أصلية وأرسلها. ارفع مستندًا آخر، وافتح تعديل البيانات واختر نوع الإشعار ثم الفاتورة الأصلية. يجب تطابق المشروع والمورد والعملة، ولا يسمح بأمر شراء مباشر للإشعار. تعرض التفاصيل صافي الأصل بعد الإشعارات وتحذر إذا أصبح سالبًا.

## الإعداد على جهاز Windows جديد

البيئة التي تحققنا منها: Python 3.13.3 وNode.js 24.13.0 وPostgreSQL 17.11. إصدارات المكتبات مثبتة في `requirements.lock.txt` و`package-lock.json`.

ثبّت Python 3.13 مع مشغّل `py` وNode.js 24 LTS، ثم شغّل `Setup_Project.cmd` من نسخة الكود. يلزم الإنترنت للإعداد الأول. السكربت يجهز بيئة Python، ويثبت الاعتماديات، ويحمل PostgreSQL المحمول من EDB مع فحص بصمته، وينشئ قواعد البيانات والحسابات المحلية، ويطبق الترحيلات ويجهز متصفح الاختبار. لا يحتاج Docker أو تثبيت خدمة PostgreSQL على مستوى الجهاز.

إذا كان Python في مسار خاص، يمكن تشغيل الإعداد من PowerShell بهذا الشكل مع استبدال مسار Python:

```powershell
Set-Location 'C:\path\to\Graduation-Project'
powershell.exe -NoProfile -ExecutionPolicy Bypass -File '.\10_Deployment\Scripts\setup-local.ps1' -PythonExecutable 'C:\path\to\python.exe'
```

على الجهاز الحالي اكتمل الإعداد. لا تحتاج تشغيل `Setup_Project.cmd` كل مرة. أوقف المشروع قبل إعادة الإعداد لأنه يعيد تثبيت اعتماديات الويب. تحققنا من إعادة الإعداد على الجهاز الحالي؛ لم تُختبر بعد آلة Windows جديدة بالكامل.

## مكان البيانات والإعدادات

| المسار | المحتوى |
|---|---|
| `.env` | اتصال التطبيق وقاعدة الاختبار ومفتاح الجلسات وإعداد المصدر. |
| `.env.example` | نموذج خالٍ من الأسرار لشرح الإعدادات. |
| `.local/runtime.json` | أسرار الإعداد المحلي المولدة. |
| `.local/DEMO_ACCOUNTS.md` | حسابات التجربة وكلمة المرور المحلية. |
| `.local/pgdata` | بيانات PostgreSQL على المنفذ 55432. |
| `.local/uploads` | أصول الملفات المرفوعة، خارج مجلد الويب العام. |
| `.local/*.log` | سجلات تشغيل الخدمات وتشخيص الأعطال. |

ملفات التشغيل المحلية والأسرار والمرفقات مستبعدة من Git. النسخ الاحتياطي لاحقًا يشمل قاعدة البيانات والمرفقات معًا؛ نسخ الكود وحده لا يحفظ الفواتير. لا تحذف `.local` لحل مشكلة تشغيل.

## التحقق بعد تغيير الكود

شغّل المشروع أولًا، ثم اختبر الجزء المتأثر. هذه الأوامر مجربة من PowerShell:

```powershell
Set-Location 'C:\path\to\Graduation-Project\04_Source_Code\backend'
& '.\.venv\Scripts\python.exe' -m pytest --tb=short
& '.\.venv\Scripts\python.exe' -m alembic check
& '.\.venv\Scripts\python.exe' -m ruff check --config pyproject.toml app tests ..\database ..\..\10_Deployment\Scripts

Set-Location 'C:\path\to\Graduation-Project\04_Source_Code\web'
npm.cmd run format:check
npm.cmd run build
npm.cmd run test:e2e

Set-Location 'C:\path\to\Graduation-Project\04_Source_Code\mobile'
npm.cmd run typecheck
npx.cmd expo-doctor
npm.cmd run build:android-bundle
```

اختبارات Python تمسح بيانات قاعدة `invoice_audit_test` المخصصة للاختبار فقط؛ لها تحقق من اسم القاعدة قبل التشغيل. اختبارات المتصفح تستخدم حسابات التطوير وتضيف مسودات مصطنعة إلى قاعدة التطوير. صور التحقق محفوظة في `.local/qa`.

عند تعديل الجداول أنشئ ترحيل Alembic جديدًا، ثم طبقه باستخدام `python -m alembic upgrade head` من مجلد الخادم. لا تعدّل قاعدة البيانات يدويًا ثم تترك الكود مختلفًا عنها.

## حل مشكلات التشغيل

إذا فشل البدء، اقرأ `.local/api.stderr.log` و`.local/web.stderr.log`، أو `.local/bootstrap.log` عند تعذر قاعدة البيانات. المنافذ 3000 و8000 و55432 يجب أن تكون متاحة للمشروع. لا توقف خدمة مجهولة تشغل المنفذ قبل التعرف عليها.

إذا ظهرت رسالة كثرة محاولات الدخول، انتظر دقيقة ثم استخدم حساب التجربة الصحيح. الجلسة مدتها ساعتان، وبعد انتهائها يلزم الدخول مجددًا. ضبط الحد الحالي لمحاولات الدخول في ذاكرة خادم واحد؛ المشاركة عبر عدة خوادم من متطلبات مرحلة النشر.

هذه بيئة تطوير محلية تستخدم HTTP وNext.js في وضع التطوير. تهيئة HTTPS والاستضافة والنسخ الاحتياطي والتشغيل الإنتاجي لها مرحلة مستقلة في الخطة.

## استخراج بيانات الفاتورة من الموقع

1. ادخل بحساب الموظف، وافتح فاتورة قابلة للتعديل ثم «تعديل بيانات الفاتورة».
2. اختر لغة OCR واضغط «استخراج البيانات». يبدأ الطلب يدويًا، وتظهر حالته حتى اكتماله. يقرأ PyMuPDF الصفحات النصية مباشرة ويستخدم PaddleOCR للمسح والصور، وتظهر طريقة قراءة كل صفحة في النتيجة. [التفاصيل والحدود](02_Requirements/Use_Cases/HYBRID_TEXT_EXTRACTION_AR.md).
3. اضغط القيم المقترحة لرؤية نص الدليل وموضعه على صورة الأصل أو صفحة PDF؛ يعرض XML مسار الحقل بدل إحداثيات صفحة.
4. اختر «نقل الاقتراحات للحقول الفارغة». اختر المورد بنفسك وصحح الحقول والبنود وأكمل الناقص.
5. حدد تأكيد المراجعة أسفل النموذج ثم «حفظ بيانات الفاتورة». الإرسال للموافقات خطوة منفصلة.

`Start_Project.cmd` يشغّل العامل مع API والويب، و`Stop_Project.cmd` يوقف شجرة عملياته. الطلبات والنتائج تبقى في PostgreSQL. مهلة القراءة 180 ثانية؛ إذا انقطع العامل تستعاد المهمة بعد انتهاء حجزها، مع حد محاولتين. ملفات المحرك مجهزة على هذا الجهاز؛ على جهاز جديد اتبع [إعداد الاستخراج](04_Source_Code/ai/document_intelligence/README_AR.md) بعد إعداد المشروع. لا تحمل أوزان النماذج ضمن Git.

سجل العامل `.local/extraction.stderr.log`، ونسخ العمل المؤقتة `.local/extraction-runs`، ومعاينات PDF الخاصة `.local/invoice-previews`. يمكن تشغيل العامل يدويًا من مجلد الخادم بالأمر `& .venv/Scripts/python.exe -m app.extraction_worker` عند عدم تشغيله من السكربت. إذا استمر «في انتظار القراءة»، تحقق من العامل وسجله؛ توفر النماذج وحده لا يعني أنه يعمل.

العينات الحالية مصطنعة؛ العربية قليلة الدقة قد تخطئ، وQR غير الواضح قد لا يُقرأ. [نتائج ربط الموقع](06_Testing_Evaluation/Results/EXTRACTION_INTEGRATION_RESULTS_AR.md)، و[قياس جودة V2](06_Testing_Evaluation/Results/OCR_QUALITY_V2_RESULTS_AR.md)، و[نتائج QR/XML](06_Testing_Evaluation/Results/STRUCTURED_SOURCES_V1_RESULTS_AR.md). يبقى التقييم الواقعي المستقل والتحقق التشفيري من العمل المتبقي.

## إعادة قراءة فاتورة بعد تحديث المحرك

في صفحة تعديل الفاتورة اختر «عربي / مختلط» للفاتورة العربية أو العربية والإنجليزية، ثم «إعادة قراءة الفاتورة» إن كانت هناك قراءة ناجحة قديمة. يتطلب تكرار الطلب مرور عشر ثوانٍ، ويبقى الطلب محفوظًا في الطابور. راجع النتيجة والأصل قبل نقل الاقتراحات وحفظها. إعادة القراءة لا تحفظ الحقول ولا تستبدل ما كتبته. لا تحتاج نسخة `0.17.1` نماذج أو اعتماديات جديدة أو ترحيل قاعدة بيانات.


## إعداد الفهم المحلي وتجربته — 0.18.0

النموذج المستخدم `qwen3-vl:8b-instruct`، مع Ollama 0.34.0 الذي اختُبر على Windows، وRTX 4070 بذاكرة 12GB وRAM 32GB. حجم النموذج نحو 6.1GB، وقد لوحظ تشغيله كاملًا على GPU. الأداء يختلف باختلاف الجهاز؛ هذا ليس حدًا أدنى مضمونًا للأجهزة الأخرى.

من جذر المشروع، بعد تثبيت Ollama، نفذ مرة واحدة:

```powershell
& ./10_Deployment/Scripts/ensure-local-vision.ps1 -DownloadModel
```

بعدها يعمل `Start_Project.cmd` كالمعتاد. سكربت البدء يشغّل خدمة محلية إذا لم تكن موجودة ويتحقق من دعم الصور؛ لا ينزّل أوزانًا أثناء القراءة. خدمة Ollama مشتركة على الجهاز وتبقى عاملة عند إيقاف المشروع. لا يغيّر الإعداد نماذج Qwen النصية المثبتة سابقًا.

من مجلد `04_Source_Code/ai/document_intelligence`:

```powershell
& ../../../.local/ocr-venv/Scripts/python.exe -X utf8 -m unittest discover -s tests -q
& ../../../.local/ocr-venv/Scripts/python.exe -X utf8 evaluate_benchmark.py --local-vision --output ../../../.local/qa/local-understanding/regression-12.json
& ../../../.local/ocr-venv/Scripts/python.exe -X utf8 evaluate_layout_challenge.py
```

`run_extraction.py` يفعّل الفهم المحلي افتراضيًا. الخيار `--no-local-vision` مخصص للمقارنة وتشخيص العطل ضمن نفس المسار. الدالة الداخلية `read_document(..., use_local_vision=False)` تُبقي الاختبارات الأساسية والتقييم القديم حتميين؛ عامل الموقع يستدعي CLI المفعّل. لا حاجة إلى SDK أو تبعيات Python إضافية؛ عميل Ollama يستخدم المكتبة القياسية، وOpenCV/Pillow موجودان ضمن بيئة Paddle.

تعني `UNAVAILABLE` تعذر طبقة الفهم فقط، و`PARTIAL` اكتمال بعض الصفحات، و`NOT_APPLICABLE` عدم وجود صفحة نصية مرئية للفهم مثل XML. افحص تشغيل Ollama وتوفر **وسم Instruct المطابق**؛ `qwen3:8b` وحده لا يكفي. إذا كانت الصورة مطموسة فلن يستعيد النموذج معلومات غير موجودة فيها، وتظل المراجعة أو صورة أوضح مطلوبة.


## تحديث القراءة الموجهة — 0.19.0

حُمل نموذج اتجاه الصفحة `PP-LCNet_x1_0_doc_ori` على هذا الجهاز، بجانب نماذج Paddle وQwen الحالية. لإعداده على جهاز آخر شغّل أمر `run_extraction.py --prepare-models` من بيئة الاستخراج المذكورة أعلاه؛ تنزيل الأوزان يحدث أثناء الإعداد فقط، ولا يحدث أثناء رفع الفاتورة. بصمات الملفات محفوظة في `model_hashes.json`. لا توجد تبعيات Python جديدة.

تستمر القراءة دون تصحيح اتجاه إذا غاب النموذج، ويظهر التحذير ضمن النتيجة. تسوية الانحناء تستخدم OpenCV وخريطة هندسية محدودة، ولا تتطلب نموذج UVDoc. لا تعالج جميع أنواع الطيات والانعكاسات.

مهلة العامل الافتراضية الحالية **420 ثانية**، وميزانية القارئ للمراحل المساعدة **380 ثانية**. إذا عُين `EXTRACTION_TIMEOUT_SECONDS` صراحة في البيئة فإن تلك القيمة تتقدم على الافتراضي. القراءة قد تستغرق دقائق عند تعدد الصفحات والقصاصات؛ الحالات الجزئية لا تمحو الاقتراحات الناجحة. أعد تشغيل `Start_Project.cmd` بعد تحديث العامل.

حدّث صفحة تعديل الفاتورة وابدأ القراءة أو اختر «إعادة قراءة الفاتورة». تعرض الشاشة اكتمال قراءة الأجزاء وبطاقات مقارنة القراءات المختلفة؛ اضغط القيمة لرؤية دليلها ثم صحح الحقل يدويًا عند الحاجة. لا تحفظ إعادة القراءة بيانات الفاتورة تلقائيًا.

الاختبار الواقعي لهذه النسخة متروك لصاحب المشروع حسب طلبه؛ التحقق البرمجي موثق في [تقرير التسليم](06_Testing_Evaluation/Results/GUIDED_INVOICE_READING_RESULTS_AR.md).
