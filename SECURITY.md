# سياسة الأمان والخصوصية

هذا المستودع عام ويحتوي كودًا وبيانات اختبار مصطنعة فقط.

## ما لا يُرفع إلى GitHub

- ملفات `.env` والأسرار وكلمات المرور ورموز الجلسات.
- قواعد PostgreSQL المحلية أو النسخ الاحتياطية منها.
- الفواتير الحقيقية أو بيانات الموردين والعملاء والمشاريع الفعلية.
- الشهادات والمفاتيح الخاصة وملفات توقيع تطبيق الجوال.
- مجلد `.local` ونتائج التشغيل والسجلات والنماذج المحملة.

ينشئ الإعداد المحلي أسرارًا عشوائية داخل `.local/runtime.json` ويولد `.env` محليًا. كلاهما مستبعد من Git.

## الإبلاغ عن مشكلة

استخدم **GitHub Private Vulnerability Reporting** أو تواصل مع مالك المستودع بصورة خاصة. لا تفتح Issue عامة تحتوي سرًا أو فاتورة أو بيانات شخصية. إذا رُفع سر بالخطأ، ألغِه أو دوّره فورًا ثم احذفه من تاريخ Git؛ حذف الملف في التزام جديد وحده لا يكفي.

## نطاق الحماية المطبق

- جلسات قابلة للإبطال وCookie من نوع HttpOnly للويب.
- تحقق Origin وCSRF لطلبات الويب التي تغير البيانات.
- Bearer Token لتطبيق الجوال مع تخزينه في SecureStore.
- فصل دور ترحيلات قاعدة البيانات عن دور التطبيق.
- منع دور التطبيق من تعديل أو حذف سجلات التدقيق المحمية.
- تحقق من نوع المرفق وحجمه ومساره وحدود معالجة المستند.
- عزل المسودات حسب المالك والمشروع والشركة وتطبيق الصلاحيات داخل FastAPI.

هذه نسخة أكاديمية محلية. أي تشغيل على شبكة عامة يحتاج HTTPS وإدارة أسرار ونسخًا احتياطية ومراقبة وتحديثًا دوريًا للاعتماديات قبل استقبال بيانات حقيقية.
